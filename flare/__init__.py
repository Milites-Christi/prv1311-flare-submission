"""
flare -- Flare hackathon build (Aug 14 deadline). Only work new for this event
lives here; it imports the hardened shared modules (screener, anomaly_gate,
footprint_gate, supabase_client, rider_decision_log) rather than copying them.
Run modules from Prv1311/ as the working directory, e.g.:
    python -m flare.divergence
"""
